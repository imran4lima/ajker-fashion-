document.addEventListener('DOMContentLoaded',()=>{const qs=new URLSearchParams(location.search),initial=qs.get('category'),search=document.getElementById('product-search'),sort=document.getElementById('sort-products'),rating=document.getElementById('rating-filter'),out=document.getElementById('shop-results');if(initial){document.querySelector(`[data-category][value="${initial}"]`)?.setAttribute('checked','checked');document.getElementById('shop-title').textContent=initial[0].toUpperCase()+initial.slice(1)}function render(){let a=getAllProducts(),q=(search.value||'').toLowerCase(),cats=[...document.querySelectorAll('[data-category]:checked')].map(x=>x.value),min=Number(rating.value);a=a.filter(p=>(!q||p.name.toLowerCase().includes(q))&&(!cats.length||cats.includes(p.category))&&p.rating>=min);if(sort.value==='newest')a.sort((x,y)=>y.createdAt.localeCompare(x.createdAt));if(sort.value==='low')a.sort((x,y)=>x.price-y.price);if(sort.value==='high')a.sort((x,y)=>y.price-x.price);if(sort.value==='rating')a.sort((x,y)=>y.rating-x.rating);if(sort.value==='discount')a.sort((x,y)=>y.discountPercentage-x.discountPercentage);out.innerHTML=a.length?a.map(productCard).join(''):'<div class="empty">কোনো পণ্য পাওয়া যায়নি। অন্য filter দিয়ে চেষ্টা করুন।</div>'}document.querySelectorAll('[data-category],#rating-filter,#product-search,#sort-products').forEach(e=>e.addEventListener('input',render));render()});
